import React from 'react'
import { Box } from '@smooth-ui/core-sc'

export const PageTitle = props => (
  <Box as="h1" fontSize="1.6rem" mb={2} fontWeight="bold" {...props} />
)

export default PageTitle
